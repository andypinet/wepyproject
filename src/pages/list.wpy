<style lang="scss">
  @import "../../node_modules/andy-aui-css/ui/xcx";

  .a-card {
    box-shadow: 0 3px 10px 1px rgba(0,0,0,0.15)
  }

  .list-item-card {
    width: 650rpx;
    height: 240rpx;
    padding: 20rpx;
    box-sizing: border-box;
    @include nth-index(1) {
      margin-top: 30rpx;
    }
    @include nth-not-last() {
      margin-bottom: 30rpx;
    }
  }
</style>
<template>
  <view class="container">
    <repeat for="{{items}}"
            index="index"
            item="item"
            key="key">
      <view class="a-card list-item-card">
        <navi :url="m1.query('/pages/listDetail', [['id', item.id]])">
          item {{item.id}}
        </navi>
      </view>
    </repeat>
  </view>
</template>
<script>
  import wepy from 'wepy'
  import commonpage from "../frame/page";
  import Navi from 'wepyk-navi'

  // import wxs
  import mywxs from '../wxs/net.wxs';

  export default class List extends commonpage {
    components = {
      navi: Navi
    };

    data = {
      items: [
        {
          id: 1
        },
        {
          id: 2
        }
      ]
    };

    wxs = {
      m1: mywxs
    };
  }
</script>
